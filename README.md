# ours-messenger-server

**`main` is intentionally empty.** The work is in **PR #1**
(`feat/messenger-server`), which is where the review belongs.

This commit exists only so that PR #1 has a base branch to target: the repository
was created empty, and GitHub cannot open a pull request without one.

Nothing is merged here. See the PR for the full summary, including the one
acceptance criterion that is UNKNOWN rather than passing, and the list of what is
not covered.
