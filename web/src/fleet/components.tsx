import { cloneElement, isValidElement, type ReactElement, type ReactNode } from 'react';
import { ChevronRight, Search } from 'lucide-react';
export function Button({ children, onClick, primary, disabled, type = 'button' }: { children: ReactNode; onClick?: () => void; primary?: boolean; disabled?: boolean; type?: 'button' | 'submit' }) { return <button type={type} className={'btn' + (primary ? ' primary' : '')} onClick={onClick} disabled={disabled}>{children}</button>; }
export function Row({ title, subtitle, onClick, avatar, end, selected }: { title: string; subtitle?: string; onClick?: () => void; avatar?: string; end?: ReactNode; selected?: boolean }) { const body = <><span className="fleet-avatar" aria-hidden>{avatar ?? title.split(' ').map(x => x[0]).join('').slice(0, 2)}</span><span className="fleet-row-copy"><strong>{title}</strong>{subtitle && <small>{subtitle}</small>}</span>{end ?? (onClick && <ChevronRight size={16} />)}</>; return onClick ? <button className={selected === undefined ? 'fleet-row' : 'fleet-row contact-row' + (selected ? ' active' : '')} aria-current={selected || undefined} onClick={onClick}>{body}{selected && <span className="contact-active-glow" aria-hidden />}</button> : <div className="fleet-row">{body}</div>; }
export function Field({ label, children }: { label: string; children: ReactNode }) { return <label className="fleet-field"><span>{label}</span>{isValidElement(children) ? cloneElement(children as ReactElement<{ 'aria-label'?: string }>, { 'aria-label': label }) : children}</label>; }
export function SearchField({ value, onChange, placeholder }: { value: string; onChange: (v: string) => void; placeholder: string }) { return <label className="fleet-search"><Search size={16} /><input aria-label={placeholder} placeholder={placeholder} value={value} onChange={e => onChange(e.target.value)} /></label>; }
export function PageHeader({ title, subtitle, onBack, actions }: { title: string; subtitle?: string; onBack?: () => void; actions?: ReactNode }) { return <header className="fleet-page-heading"><div>{onBack && <Button onClick={onBack}>‹ Back</Button>}<h1>{title}</h1>{subtitle && <p>{subtitle}</p>}</div><div className="fleet-actions">{actions}</div></header>; }
