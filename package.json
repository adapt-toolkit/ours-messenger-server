{
  "name": "@ours.network/messenger-server",
  "version": "0.1.0",
  "description": "Self-hosted messenger backend for ours.network with an isolated embedded SDK runtime, REST/SSE API, focused web client, and WebPush.",
  "type": "module",
  "license": "FSL-1.1-Apache-2.0",
  "author": "Adapt Toolkit",
  "keywords": [
    "ours",
    "a2a",
    "adapt",
    "e2e",
    "messaging",
    "messenger",
    "webpush",
    "rest"
  ],
  "bin": {
    "ours-messenger-server": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "tsx src/cli.ts serve",
    "serve": "node dist/cli.js serve",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && for f in tests/owned-runtime.test.mjs tests/runtime-lock.test.mjs tests/lifecycle-gate.test.mjs tests/sdk-signal-ownership.blocker.test.mjs tests/bundle-smoke.test.mjs tests/conversation-page.test.mjs tests/state-dir-isolation.test.mjs tests/events.test.mjs tests/sse-e2e.test.mjs tests/web-contract.test.mjs tests/receipts.test.mjs tests/rest-e2e.test.mjs web/tests/read-gate.test.ts; do echo \"# $f\"; node --import tsx \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@ours.network/sdk": "1.0.1",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/web-push": "^3.6.4",
    "esbuild": "^0.24.2",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
